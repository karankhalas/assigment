//9. .find the area of a rectangular prism formula : A=2(wl+hl+hw)

#include<stdio.h>
main()
{
	float lenght,wodth,area;
	
	printf("\nEnter of ractangle");
	scanf("%f",&length);
	
	printf("\nEnter width of rectangle");
	scanf("%f",&width);
	
	area=length*width;
	
	printf("Area of Rectangle is %f",area);
}

